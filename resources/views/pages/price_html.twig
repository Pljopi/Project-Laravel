{% extends "layouts/base_html.twig" %} {% block CONTENT %}

<div class="content">
    <h1>
        Spot price of
        {{ criptoCurrency }}
        is
        {{ price }}
        {{ currency }}
    </h1>
    <h2>
        {{ post.published_at|date("d/m/Y") }}
        {{ post.published_at|date("H:i:s") }}
    </h2>
</div>

{% endblock %}
